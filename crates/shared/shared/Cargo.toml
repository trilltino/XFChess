[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.19.0-rc.3", default-features = false }
serde = { version = "1.0", features = ["derive"] }
uuid = { version = "1.10", features = ["serde", "v4", "js"] }

[dev-dependencies]
bincode = "1.3"
