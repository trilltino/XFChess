{
  "identifier": "default",
  "description": "Base permissions for all XFChess windows. Shell access is intentionally NOT granted here — it is scoped to the tournament-admin window only via admin-shell.json.",
  "windows": ["main", "tournament-admin", "wallet-popup"],
  "permissions": [
    "core:default"
  ]
}
