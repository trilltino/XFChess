{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the visualiser window (invoke commands + listen to events).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
