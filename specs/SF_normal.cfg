\* Phase 4: Solana settlement with the real nonce + parent_nonce checks,
\* Byzantine submitters and a reordering mempool.
\* Expect: the committed chain is always linearizable (no fork, no gap).
SPECIFICATION Spec

CONSTANTS
    Agents      = {A, B}
    Outsiders   = {}
    Authorized  = {A, B}
    MaxNonce    = 3
    MaxContent  = 2
    Byzantine   = {A}
    SubmitCap   = 5
    EnforceNonce = TRUE
    EnforceAuth  = TRUE

INVARIANT TypeOK
INVARIANT ChainLinearizable
INVARIANT OnlyAuthorizedCommitted
