\* Phase 2: peer A is Byzantine and equivocates.
\* Models the CURRENT genesis-bypass code (GenesisBypass = TRUE), auth on.
\* RESULT: NoFork is VIOLATED via the parent_version = "0" bypass.
SPECIFICATION SafeSpec

CONSTANTS
    Agents      = {A, B}
    MaxSeq      = 2
    MaxContent  = 2
    Byzantine   = {A}
    CheckParent   = TRUE
    GenesisBypass = TRUE
    AuthBinding   = TRUE
    EnableAdversary = FALSE

INVARIANT TypeOK
INVARIANT NoFork
INVARIANT SeqMonotonic
INVARIANT NoEquivocationAccepted
INVARIANT OnlyAuthenticAccepted
