\* Models the FIX: game_log.rs checks a per-game participant roster (built
\* from ChessMessage::SessionInfo posts, mirroring CausalChainState.roster)
\* before accepting a Move/Resign/draw-offer PUT.
\* EXPECTED RESULT: OnlyParticipantsAccepted holds.
SPECIFICATION Spec

CONSTANTS
    Agents      = {A, B}
    Outsiders   = {C}
    MaxSeq      = 3
    MaxContent  = 3
    AuthCheck   = TRUE
    DedupPresent = TRUE

INVARIANT TypeOK
INVARIANT OnlyParticipantsAccepted
