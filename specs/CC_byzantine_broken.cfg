    \* Necessity proof: equivocation guard REMOVED (CheckParent = FALSE).
    \* RESULT: NoFork is VIOLATED. Confirms the guard is load-bearing.
    SPECIFICATION SafeSpec

    CONSTANTS
        Agents      = {A, B}
        MaxSeq      = 2
        MaxContent  = 2
        Byzantine   = {A}
        CheckParent   = FALSE
        GenesisBypass = TRUE
        AuthBinding   = TRUE
        EnableAdversary = FALSE

    INVARIANT TypeOK
    INVARIANT NoFork
    INVARIANT SeqMonotonic
    INVARIANT NoEquivocationAccepted
    INVARIANT OnlyAuthenticAccepted
