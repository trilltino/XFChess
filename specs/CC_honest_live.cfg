\* Phase 3: liveness. No permanent loss + weak fairness.
\* Expect: both peers' full move streams eventually converge.
SPECIFICATION LiveSpec

CONSTANTS
    Agents      = {A, B}
    MaxSeq      = 3
    MaxContent  = 3
    Byzantine   = {}
    CheckParent   = TRUE
    GenesisBypass = TRUE
    AuthBinding   = TRUE
    EnableAdversary = FALSE

PROPERTY Convergence
