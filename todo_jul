SOLANA NETWORKING
- finalize_game e2e: release wager escrow to winner after game ends, show "Prize claimed" in post-game UI
- undelegation gate: poll Game PDA delegated flag, only call finalize_game once ER has returned the account
- session key expiry UX: check expires_at at GameStartedEvent, prompt re-authorize if < 24h remaining
- free rated ELO submission: after Free Rated game ends, POST to /ratings/update to record result and update ELO
- record_move nonce resync: on reconnect, fetch on-chain move_log.nonce via RPC and reset local move_nonce to match
- dispute UI: add Dispute button in post-game screen for wager games, wire to dispute instruction
- tournament session routing: detect tournament match from pairing result, route through session_create_game / session_join_game
- global session VPS handshake: verify full round-trip client -> backend /sign/global_create -> client assembles tx -> broadcast -> confirm

P2P LOBBY
- show host ELO rating in each lobby card
- lobby capacity badge (1/2 players)
- filter/sort bar: by time control, stake, ELO range
- "Loading..." skeleton while fetching lobby list
- lobby expiry countdown (auto-remove stale rooms after timeout)
- friends invite: send a join link to a friend by pubkey / handle
- password-protected private room option
- region ping indicator (us/eu/asia tag from VPS metadata)
- pre-game lobby chat (text exchange before both accept start)


SOLANA LOBBY
- browse open wagered games list (filtered to solana_wager type, like P2P lobby)
- game ID display + copy while waiting for opponent
- USD equivalent shown next to wager SOL amount
- time control selector on Create tab (currently hardcoded 300s/0 inc)
- session key status chip: Authorized / Expired / Not set — with Authorize button
- session key expiry warning: prompt re-authorize if < 24h remaining
- reconnect flow: if game PDA already exists for this wallet show Rejoin button
- dispute entry point: Dispute button post-game for wager games
- pot-size preview (show my escrow + opponent escrow = total pot)
- ELO matching preference toggle: strict / expanded / any
- tournament routing: detect pairing from /tournament/match, auto-route to session_create_game / session_join_game
- spectator mode: view ongoing solana games with live move feed

SOLANA CONTRACT

HERTNZER SERVER

ADMIN PANEL
  SIGNING MODEL: Bearer token auth only — backend signs all on-chain ops with
  vps_authority keypair loaded from .env. No wallet popup ever. On-chain actions
  (cancel, fund-prize) return a partially-signed tx; admin completes via CLI or
  Tauri shell command (solana sign-and-send), not a browser extension.

  TOURNAMENT MANAGEMENT  [tauri/tournament-admin → TournamentDetail.tsx / TournamentList.tsx]
  [✅ DONE] "Start Swiss" button → POST /admin/tournament/{id}/initialize-swiss (no signing, pure state)
  [✅ DONE] current round pairings display: show white vs black per match in the Matches tab
  [✅ DONE] record result form per match: White wins / Black wins / Draw → POST /admin/tournament/{id}/record-result (no signing)
  [✅ DONE] standings panel: Swiss points table with Buchholz + Sonneborn tiebreaker columns
  [ ] auto-advance round indicator: after all results submitted show "Advancing to Round N…" status line; poll bracket until new pairings appear — backend auto-starts next round via SwissService.start_round; UI just needs to detect when current_round increments
  [ ] "Advance Round" manual override button: POST /admin/tournament/{id}/advance-round (backend endpoint needed) — only shown when all matches in current round are Completed but current_round hasn't incremented yet (stuck state)
  [ ] bracket visualizer for single-elimination: render a binary tree from the matches array; left branch = match 0..n/2-1, right branch fills up to final; completed nodes show winner name; TournamentDetail → Matches tab, SE format only
  [ ] bye assignment display: in Swiss round pairings show "BYE" row for player who has no opponent that round; pulled from swiss_data.rounds[r].byes array; auto-award +1 point notation in standings
  [ ] forfeit / no-show handling: "Forfeit" button per pending match → calls record-result with opponent as winner + sets reason=forfeit in body; add reason field to RecordResultReq in backend
  [ ] cancel tournament: button on overview tab → POST /admin/tournament/{id}/cancel → backend returns base64 partially-signed tx → display tx in monospace box with "Copy TX" button for CLI broadcast; no popup
  [ ] fund USDC prize pool: form on overview tab (amount field in USDC) → POST /admin/tournament/{id}/fund-prize-tx → display partially-signed tx for CLI broadcast; no popup; shows current usdc_prize_pool balance alongside
  [ ] set match game ID: input field per match card in Matches tab → POST /admin/tournament/{id}/set-match-game-id; links live on-chain game to tournament pairing slot
  [ ] tournament templates: save current CreateTournament form state to localStorage as named preset; load dropdown in CreateTournament header; no backend needed
  [ ] player re-seeding: drag-and-drop list in Players tab before tournament starts; reorder players array client-side → POST /admin/tournament/{id}/reseed with new ordered player list (backend endpoint needed)
  [ ] round timer display: show per-round deadline if set; countdown chip in Matches tab header; auto-forfeit fires on backend scheduler — UI reads round_deadline_at from tournament detail and renders live countdown
  [ ] vps_authority key rotation: dedicated Settings page → POST /admin/keys/rotate-authority (backend endpoint needed, hot-swaps keypair from new .env value without restart); CLI fallback documented
  [ ] admin token rotation: Settings page → POST /admin/auth/rotate-token → invalidates current Bearer token and returns new one; update localStorage automatically
  [ ] tournament status filter in list: filter chips on TournamentList (Registration / Active / Completed / Cancelled) — client-side filter on fetched list, no new endpoint needed
  [ ] bulk register players: CSV paste field in Players tab of tournament detail → parse wallet addresses → call POST /tournament/{id}/join for each; show per-row success/fail status

  PLAYER MANAGEMENT  [tauri/tournament-admin → PlayerList.tsx + new PlayerDetail page]
  [ ] player profile detail page: click player row in PlayerList → expand or navigate to PlayerDetail; show ELO history sparkline (GET /admin/players/{wallet}/history), win/loss/draw counts, active bans, registered tournaments
  [ ] manual ELO adjustment: input field + reason textarea in player detail → POST /admin/players/{wallet}/elo-override {new_elo, reason} (no signing); reason written to audit log; current ELO shown alongside
  [ ] ban / suspend player: "Ban" button in player detail → POST /admin/players/{wallet}/ban {reason, duration_days: null=permanent} (no signing); propagates to matchmaking block; ban shown as red badge in PlayerList
  [ ] player search & filter: search bar on PlayerList with filter chips for ELO range, KYC status, ban status, country; client-side filter on loaded list or query params on GET /admin/players?elo_min=&kyc=&banned=
  [ ] export player list: "Export CSV" button on PlayerList → downloads wallet,username,elo,kyc_status,ban_status CSV; use existing GET /admin/players endpoint data + browser Blob download, no new endpoint
  [ ] bulk import players for tournament: CSV upload widget in tournament Players tab → parse addresses → loop POST /tournament/{id}/join; progress bar per row; skip duplicates

  MATCH ADMINISTRATION  [tauri/tournament-admin → MatchManagement.tsx + GameExplorer.tsx]
  [ ] live spectator link in match cards: "SPECTATE" button on each active session card in MatchManagement → opens deep-link xfchess://spectate/{game_id} via Tauri shell or copies link; requires spectate mode in game client
  [ ] force resign: "Force Resign" button in active match card → confirm dialog → POST /admin/games/{game_id}/force-resign {winner} (backend signs with vps_authority, no popup); updates game PDA result on-chain
  [✅ DONE] move log viewer: GameExplorer lets admin search by wallet/username and view per-game move history with timestamps
  [ ] FEN scrubber / position replay: in GameExplorer move log view add a position board preview; slider through move list rebuilds FEN client-side using nimzovich_engine WASM or calls GET /games/{game_id}/fen/{move_index}; renders board SVG or 2D canvas
  [ ] flag game for review: "Flag" button per game in GameExplorer → POST /admin/games/{game_id}/flag {reason} (no signing); game appears in Dashboard MODERATION tab queue
  [ ] engine eval deviation graph: in MODERATION tab dispute card render a line chart of centipawn eval vs move number; data from GET /admin/anti-cheat/game/{game_id}/eval (backend endpoint to run stockfish eval needed)

  FINANCIAL / TREASURY  [tauri/tournament-admin → Dashboard.tsx + new Treasury page]
  [✅ DONE] treasury balance dashboard: Dashboard shows live SOL balance for feepayer, vps_signer, kyc_signer, treasury wallets — fetched from GET /admin/wallet-balances every 5s
  [✅ DONE] SOL/USD rate display: Dashboard shows SOL exchange rates from GET /api/rates/all (Jupiter-backed)
  [ ] prize distribution log: Treasury page table of all completed tournament payouts; columns: tournament name, place, winner wallet, amount SOL, amount USDC, tx signature; GET /admin/treasury/payouts (backend endpoint needed — query sessions.db for finalize_game events)
  [ ] fee revenue report: chart of platform_fee_pool collected per tournament and per day/week; GET /admin/treasury/fee-report?period=week (backend endpoint needed); display as bar chart or table with totals
  [ ] manual refund: form on Treasury page (wallet + amount + reason) → POST /admin/treasury/refund {wallet, lamports, reason} → backend builds + signs tx with vps_authority → returns base64 tx for CLI broadcast; no popup
  [ ] feepayer top-up alert: configurable threshold (default 0.5 SOL) in Settings → when feepayer balance < threshold show red banner in Dashboard header with "TOP UP REQUIRED" + current balance; threshold stored in localStorage
  [ ] escrow balance per tournament: in tournament overview ECONOMICS card show live escrow PDA SOL balance from GET /admin/tournament/{id}/escrow-balance (backend endpoint: fetch lamports from escrow PDA via RPC)
  [ ] USDC prize pool balance: alongside fund-prize button show current usdc_prize_escrow token account balance fetched from RPC

  ANTI-CHEAT & MODERATION  [tauri/tournament-admin → Dashboard.tsx MODERATION tab]
  [✅ DONE] dispute queue: Dashboard MODERATION tab lists flagged games from GET /admin/anti-cheat/reports with verdict + suspect + reason
  [✅ DONE] resolve dispute: "False Positive" and "Refund Victim" buttons call ResolveDispute on-chain via backend (currently alerts, needs real endpoint wiring to POST /admin/disputes/{game_id}/resolve)
  [ ] engine eval deviation graph per flagged game: line chart of centipawn loss per move; requires backend to store/return eval data; GET /admin/anti-cheat/game/{game_id}/eval; render with recharts or lightweight canvas
  [ ] dispute timer: show how long each dispute has been open (created_at → now); disputes open > 48h highlighted in amber
  [ ] dispute assignment: "Assign to me" button per dispute row → POST /admin/disputes/{game_id}/assign {reviewer: token_owner}; assigned reviewer shown as badge; prevents double-resolution
  [ ] IP ban: form field (IP address + reason) → POST /admin/moderation/ip-ban {ip, reason} (backend state update, no signing); banned IPs shown in a list on Moderation tab with unban button
  [ ] rate-limit override: "Whitelist" button per player in PlayerList → POST /admin/moderation/whitelist {wallet} (no signing); removes from IP rate-limit checks
  [ ] audit log viewer: dedicated Audit Log tab or page; GET /admin/audit-log?limit=100 (backend endpoint needed — append to SQLite audit_log table on every admin action); columns: timestamp, actor token, action, target, result; infinite scroll

  INFRASTRUCTURE  [tauri/tournament-admin → Dashboard.tsx + HetznerSsh.tsx + DeploymentManager.tsx]
  [✅ DONE] backend health dashboard: Dashboard shows active sessions, total games, confirmed txs, CPU%, RAM% from Prometheus at port 9090
  [✅ DONE] deploy pipeline: DeploymentManager page + "Rollout Update" button in Dashboard runs deploy.bat sidecar
  [✅ DONE] platform wallet balances: Dashboard sidebar shows feepayer/vps_signer/kyc_signer/treasury balances live
  [ ] live backend log stream: Dashboard CONSOLE tab currently shows deployment logs only — extend to tail actual backend logs via GET /admin/logs/stream (SSE endpoint needed on backend, tails the tracing output); keep last 200 lines, color-code WARN/ERROR/INFO
  [ ] scheduled task status: Dashboard sidebar widget showing tournament-scheduler heartbeat (last_tick timestamp) and matchmaking heartbeat; GET /admin/tasks/status (backend endpoint exposing task liveness); red if > 60s stale
  [ ] database size monitor: GET /admin/db/stats → {sessions_db_bytes, vault_db_bytes, sessions_db_rows, vault_db_rows}; display in Dashboard sidebar with growth indicator (compare to previous poll)
  [ ] cert expiry monitor: GET /admin/tls/expiry → [{domain, expires_at, days_remaining}]; display in Infrastructure tab; red if < 14 days; backend reads cert from disk or calls openssl
  [ ] deploy history log: track last 10 deploys in localStorage (timestamp, commit hash, exit code); show in DeploymentManager as a table above the deploy button
  [ ] Hetzner SSH terminal: HetznerSsh.tsx exists — wire up actual shell via @tauri-apps/plugin-shell to ssh into 178.104.55.19; show output stream in terminal widget with command input
  [ ] node status liveness: Dashboard sidebar "ACTIVE NODES" section currently hardcoded ONLINE — replace with real health checks: GET /health for backend, GET http://{ip}:9100/metrics for node_exporter existence; update every 30s

Tournaments LOBBY

IN GAME UI 3D

IN GAME UX 2D

END GAME UI/UX

CHESS P2P PGN REPORT
- assemble PGN from MoveHistory at game end (client-side, no backend needed)
- post-game overlay: PGN text box + "Copy PGN" button + "Download .pgn" button
- send completed PGN to backend POST /pgn/submit for archival (new endpoint)

CHESS SOLANA PGN REPORT
- game over screen: fetch GET /games/{game_id}/pgn after finalize_game completes
- display PGN in scrollable text panel with "Copy" button
- show player names and result in the PGN header tags

CHESS TOURNEY PGN REPORT
- GET /tournament/{id}/pgn — aggregate all match PGNs as one downloadable file
- tournament results UI: per-match "View PGN" button
- confirm tournament games push moves through the DB recording path

PREFORMANCE & OPTIMIZATION

Game Launch Trailer
